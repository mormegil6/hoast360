diff --git a/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.cjs.js b/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.cjs.js
index 66ad1c6..5e18340 100644
--- a/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.cjs.js
+++ b/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.cjs.js
@@ -37,6 +37,18 @@ function (_MenuItem) {
   var _proto = SourceMenuItem.prototype;
 
   _proto.handleClick = function handleClick() {
+    // Deselect every sibling before selecting this one. video.js MenuItem marks
+    // the clicked item selected but never clears the previous, and the owning
+    // MenuButton only rebuilds items when the level LIST changes - which it does
+    // not here - so each click left another entry highlighted and the menu showed
+    // several resolutions as current at once.
+    var _menu = this.parentComponent_;
+    if (_menu && typeof _menu.children === "function") {
+      var _self = this;
+      _menu.children().forEach(function (child) {
+        if (child !== _self && typeof child.selected === "function") child.selected(false);
+      });
+    }
     var selected = this.options_;
     console.log("Changing quality to:", selected.label);
 
@@ -121,6 +133,11 @@ function (_MenuButton) {
     var menuItems = [];
     var levels = this.player().qualityLevels();
     var labels = [];
+    // Does any rung actually match what is playing? If not, the menu would open
+    // with NOTHING ticked, which is what users saw: no resolution selected until
+    // they picked one by hand. Auto is the honest answer in that case - it IS
+    // what the player is doing.
+    var anySelected = false;
 
     for (var i = 0; i < levels.length; i++) {
       var index = levels.length - (i + 1);
@@ -143,6 +160,8 @@ function (_MenuButton) {
       }
 
       labels.push(label);
+
+      if (selected) { anySelected = true; }
       menuItems.push(new SourceMenuItem(this.player_, {
         label: label,
         index: index,
@@ -156,7 +175,7 @@ function (_MenuButton) {
       menuItems.push(new SourceMenuItem(this.player_, {
         label: 'Auto',
         index: levels.length,
-        selected: false,
+        selected: !anySelected,
         sortVal: 99999
       }));
     } // Sort menu items by their label name with Auto always first
diff --git a/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.es.js b/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.es.js
index a3e1b60..77dab63 100644
--- a/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.es.js
+++ b/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.es.js
@@ -33,6 +33,18 @@ function (_MenuItem) {
   var _proto = SourceMenuItem.prototype;
 
   _proto.handleClick = function handleClick() {
+    // Deselect every sibling before selecting this one. video.js MenuItem marks
+    // the clicked item selected but never clears the previous, and the owning
+    // MenuButton only rebuilds items when the level LIST changes - which it does
+    // not here - so each click left another entry highlighted and the menu showed
+    // several resolutions as current at once.
+    var _menu = this.parentComponent_;
+    if (_menu && typeof _menu.children === "function") {
+      var _self = this;
+      _menu.children().forEach(function (child) {
+        if (child !== _self && typeof child.selected === "function") child.selected(false);
+      });
+    }
     var selected = this.options_;
     console.log("Changing quality to:", selected.label);
 
@@ -117,6 +129,11 @@ function (_MenuButton) {
     var menuItems = [];
     var levels = this.player().qualityLevels();
     var labels = [];
+    // Does any rung actually match what is playing? If not, the menu would open
+    // with NOTHING ticked, which is what users saw: no resolution selected until
+    // they picked one by hand. Auto is the honest answer in that case - it IS
+    // what the player is doing.
+    var anySelected = false;
 
     for (var i = 0; i < levels.length; i++) {
       var index = levels.length - (i + 1);
@@ -139,6 +156,8 @@ function (_MenuButton) {
       }
 
       labels.push(label);
+
+      if (selected) { anySelected = true; }
       menuItems.push(new SourceMenuItem(this.player_, {
         label: label,
         index: index,
@@ -152,7 +171,7 @@ function (_MenuButton) {
       menuItems.push(new SourceMenuItem(this.player_, {
         label: 'Auto',
         index: levels.length,
-        selected: false,
+        selected: !anySelected,
         sortVal: 99999
       }));
     } // Sort menu items by their label name with Auto always first
diff --git a/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.js b/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.js
index 642ee73..b3fef56 100644
--- a/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.js
+++ b/node_modules/videojs-http-source-selector/dist/videojs-http-source-selector.js
@@ -45,6 +45,18 @@
     var _proto = SourceMenuItem.prototype;
 
     _proto.handleClick = function handleClick() {
+    // Deselect every sibling before selecting this one. video.js MenuItem marks
+    // the clicked item selected but never clears the previous, and the owning
+    // MenuButton only rebuilds items when the level LIST changes - which it does
+    // not here - so each click left another entry highlighted and the menu showed
+    // several resolutions as current at once.
+    var _menu = this.parentComponent_;
+    if (_menu && typeof _menu.children === "function") {
+      var _self = this;
+      _menu.children().forEach(function (child) {
+        if (child !== _self && typeof child.selected === "function") child.selected(false);
+      });
+    }
       var selected = this.options_;
       console.log("Changing quality to:", selected.label);
 
@@ -129,6 +141,11 @@
       var menuItems = [];
       var levels = this.player().qualityLevels();
       var labels = [];
+      // Does any rung actually match what is playing? If not, the menu would open
+      // with NOTHING ticked, which is what users saw: no resolution selected until
+      // they picked one by hand. Auto is the honest answer in that case - it IS
+      // what the player is doing.
+      var anySelected = false;
 
       for (var i = 0; i < levels.length; i++) {
         var index = levels.length - (i + 1);
@@ -151,6 +168,8 @@
         }
 
         labels.push(label);
+
+        if (selected) { anySelected = true; }
         menuItems.push(new SourceMenuItem(this.player_, {
           label: label,
           index: index,
@@ -164,7 +183,7 @@
         menuItems.push(new SourceMenuItem(this.player_, {
           label: 'Auto',
           index: levels.length,
-          selected: false,
+          selected: !anySelected,
           sortVal: 99999
         }));
       } // Sort menu items by their label name with Auto always first
diff --git a/node_modules/videojs-http-source-selector/src/components/SourceMenuButton.js b/node_modules/videojs-http-source-selector/src/components/SourceMenuButton.js
index d810276..9058608 100644
--- a/node_modules/videojs-http-source-selector/src/components/SourceMenuButton.js
+++ b/node_modules/videojs-http-source-selector/src/components/SourceMenuButton.js
@@ -48,6 +48,11 @@ class SourceMenuButton extends MenuButton
     var menuItems = [];
     var levels = this.player().qualityLevels();
     var labels = [];
+    // Does any rung actually match what is playing? If not, the menu would open
+    // with NOTHING ticked, which is what users saw: no resolution selected until
+    // they picked one by hand. Auto is the honest answer in that case - it IS
+    // what the player is doing.
+    var anySelected = false;
 
     for (var i = 0; i < levels.length; i++) {
       var index = levels.length - (i + 1);
@@ -69,13 +74,14 @@ class SourceMenuButton extends MenuButton
         continue
       }
       labels.push(label);
+      if (selected) { anySelected = true; }
 
       menuItems.push(new SourceMenuItem(this.player_, { label, index, selected, sortVal }));
     }
 
     // If there are multiple quality levels, offer an 'auto' option
     if (levels.length > 1) {
-      menuItems.push(new SourceMenuItem(this.player_, { label: 'Auto', index: levels.length, selected: false, sortVal: 99999 }));
+      menuItems.push(new SourceMenuItem(this.player_, { label: 'Auto', index: levels.length, selected: !anySelected, sortVal: 99999 }));
     }
 
     // Sort menu items by their label name with Auto always first
diff --git a/node_modules/videojs-http-source-selector/src/components/SourceMenuItem.js b/node_modules/videojs-http-source-selector/src/components/SourceMenuItem.js
index 40cf058..2a45897 100644
--- a/node_modules/videojs-http-source-selector/src/components/SourceMenuItem.js
+++ b/node_modules/videojs-http-source-selector/src/components/SourceMenuItem.js
@@ -12,6 +12,18 @@ class SourceMenuItem extends MenuItem
   }
 
   handleClick() {
+    // Deselect every sibling before selecting this one. video.js MenuItem marks
+    // the clicked item selected but never clears the previous, and the owning
+    // MenuButton only rebuilds items when the level LIST changes - which it does
+    // not here - so each click left another entry highlighted and the menu showed
+    // several resolutions as current at once.
+    var _menu = this.parentComponent_;
+    if (_menu && typeof _menu.children === "function") {
+      var _self = this;
+      _menu.children().forEach(function (child) {
+        if (child !== _self && typeof child.selected === "function") child.selected(false);
+      });
+    }
     var selected = this.options_;
     console.log("Changing quality to:", selected.label);
     super.handleClick();
